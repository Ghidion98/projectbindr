name: Deploy Umbraco 15 to Azure Web App (.NET 9)

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET 9 (Preview)
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore ./Umbraco15web/Umbraco15web.csproj

      - name: Build the project
        run: dotnet build ./Umbraco15web/Umbraco15web.csproj --configuration Release --no-restore

      - name: Publish the app
        run: dotnet publish ./Umbraco15web/Umbraco15web.csproj -c Release -o ./publish

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'projectbindr'  # must match your Azure Web App name
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: ./publish
